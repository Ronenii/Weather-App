export interface Weather {
  temperature: number;
  imageUrl: string;
  date: string;
  condition: string;
  last_update: string;
  city: string;
  region: string;
  state: string;
  max_temp: number;
  min_temp: number;
}
